# HeatSim — инструмент расчёта двухпоточного теплообмена

**Версия: 1.6**

## Краткий Changelog (v1.6)

- Раздельные графики компонентов теперь открываются в отдельном немодальном окне сбоку от окна анализа; основное окно остаётся доступным для редактирования параметров.
- Кнопка «Редактировать» переводит в режим редактирования обе таблицы (горячую и холодную) одновременно.
- Исправлено поведение переключателя «Разделить компоненты по графикам»: при закрытии бокового окна галочка автоматически снимается и остаётся работоспособной; после редактирования можно снова включить разделение и получить графики.
- Устранено исчезновение серий при открытом окне раздельных графиков — кривые строятся для всех компонентов.
- Вернули гибкий размер трёх основных графиков (убран фиксированный размер).

## Краткий Changelog (v1.5)

- Окно анализа смесей:
	- Живой индикатор «Остаток доли»/«Перебор» под каждой таблицей.
	- Разрешено временно превышать сумму долей при редактировании; строгое равенство сумме 1.0 проверяется на «Утвердить».
	- Автоформат чисел долей без хвостовых нулей (например, 0.800000 → 0.8); поддержка точки и запятой.
	- Кнопка «Построить графики» теперь во всю ширину; переключатель «Разделить компоненты по графикам» на одной строке с кнопкой.
	- (устарело в v1.6) Ранее разделённые графики строились inline. Начиная с v1.6 выводится отдельное боковое окно раздельных графиков.
- Ограничение ввода долей: только числа в [0..1].
- В главном окне: минимальный размер, центрирование, меню «Вид → Сбросить вид», панель гидросхем/выходов расширяется по ширине, изображение гидросхем фиксированного размера.

## Краткий Changelog (v1.4)

- Улучшена логика показа окна анализа: окно открывается только при наличии рассчитанных Q, σ и K.
- Улучшена валидация импортируемых смесей и добавлена более строгая проверка входных данных.
- Визуальные правки: выравнивание элементов в гидродинамической панели и переработка кнопки "Перерасчёт"; ширина селектора компонентов увеличена до 300px.
- Экспорт входных смесей: нули экспортируются как пустые строки.
- База компонентов: переход на импорт/экспорт через Excel (.xlsx); CSV-экспорт базы удалён (чтобы избежать проблем с кодировкой и схемой столбцов).
- Автозагрузка справочника: при старте приоритетно загружается `data/components.xlsx` (если есть). При отсутствии — производится попытка чтения `data/components.csv` или `data/components_nist_results.csv` (этот файл содержит только ссылки-источники к компонентам).
- Релизы и CI: добавлен workflow для автосборки и публикации релиза по пушу тега `v*`; скрипт `upload-release.ps1` облегчает создание релиза и загрузку ассетов (требуется GitHub PAT или `GITHUB_TOKEN`).
- Резервное копирование: создан и зафиксирован в репозитории бэкап-снимок (ZIP) и создан тег `v1.4`. Рекомендуется не хранить большие бинарные архивы в основном репозитории — лучше прикреплять их как ассеты релиза или размещать в релизе на GitHub.

Кратко: это локальное Qt‑приложение на Python для расчёта теплообменных задач между горячим и холодным потоками с возможностью задания составов смесей компонентов.

**Содержание README**
- **Возможности**
- **Установка**
- **Запуск**
- **Интерфейс**
- **Анализ долей компонентов**
- **Импорт / Экспорт**
- **Логи**
- **Резервное копирование**
- **Сборка .exe**
- **Публикация релиза**
- **Отладка**

## Возможности

- Расчёт тепловой нагрузки `Q`, предполагаемый выходной температурный баланс для горячего потока (`t_out_plus`), приблизительное значение UA (`k`, кВт/K) по методу LMTD и удельной скорости производства энтропии `sigma`.
- Поддержка нескольких гидродинамических схем.
- Конструктор смесей для горячего и холодного потоков с валидацией сумм долей.
- Режим справочника компонентов и ручной ввод свойств (`Tb`, `C_f`, `C_p`, `r_f`).
- Лок‑кнопки для «заморозки» введённых значений.
- Авто‑нормализация числового ввода и мягкий авто‑пересчёт отдельных зависимых полей.
- Окно анализа «Анализ изменения долей компонентов смеси» с построением графика зависимости `Q – σ` при вариации долей.
- Меню «Помощь»: справка, просмотр логов и окно «О программе» (версия из файла `VERSION`).
- Импорт / экспорт входных данных (CSV / Excel) без нежелательных преобразований форматов чисел и дат.
- Импорт / экспорт базы компонентов через Excel (.xlsx); автозагрузка при старте из `data/components.xlsx` при наличии.
- Кнопка «Очистить параметры» сбрасывает состояние, предотвращая случайный пересчёт до явного действия.
- Локализация диалога удаления строк на «Да / Нет».
- Логирование в `logs/app.log`.
- Скрипт резервного копирования `backup.ps1` (периодичность через Планировщик задач) + журнал `backup_history.log`.

## Требования
- Python 3.11+ (рекомендовано)
- Windows (инструкции приведены для PowerShell)

Уже подготовлен `requirements.txt` с нужными версиями пакетов.

## Установка

### Создание виртуального окружения и установка зависимостей

Откройте PowerShell в корне проекта (`c:\Users\ruben\OneDrive\Documents\Projects\Python\HeatSim`) и выполните:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

Это создаст виртуальное окружение `.venv` и установит все зависимости (включая `PyQt5`).

Если вы уже активировали окружение, убедитесь, что в терминале виден путь к `.venv`.

## Запуск

После активации окружения запустите приложение:

```powershell
python main.py
```

Окно приложения — графический интерфейс на PyQt5.

## Интерфейс

- Панель горячего и холодного потока (`FlowPanel`):
	- `T_in`, `T_out` — входная и выходная температуры в K
	- `m` — массовый расход (кг/с)
	- `p` — давление (кг/м²)
	- Для каждого поля есть кнопка «🔒/🔓» — блокировать/разблокировать поле после редактирования.

- Панель смеси (`MixPanel`):
	- Выбор компонента из справочника (список хранится в `interface.py` в `COMPONENT_DB`).
	- `Доля` — массовая доля компонента в смеси (ожидается сумма ≈ 1.0 для корректных расчётов).
	- Поля `Tb`, `C_f`, `C_p`, `r_f` заполняются автоматически из справочника или вводятся вручную при выборе соответствующего режима.
	- Таблица списка компонентов смеси с суммами и возможностью удаления строк.

- Кнопки управления: добавление компонентов в смесь, импорт/экспорт смесей в CSV (если требуется), запуск расчётов и вывод результатов.

## Что делает логика (`logic.py`)

- Вычисляет: тепловой поток `Q` (кВт), предполагаемый выходной температурный баланс для горячего потока (`t_out_plus`), приблизительное значение UA (`k`, кВт/K) по методу LMTD и удельную скорость производства энтропии `sigma`.
- Смеси обрабатываются простым взвешенным усреднением теплоёмкостей (выбираются `C_f` или `C_p` в зависимости от сравнения с температурой кипения `Tb`).

## Анализ долей компонентов

Кнопка «Провести анализ» открывает отдельное окно со следующей логикой:
- Две таблицы — копии смесей горячего и холодного потоков. Редактирование долей не влияет на исходные данные главного окна.
- Под каждой таблицей отображается «Остаток доли» либо «Перебор», обновляется в реальном времени при наборе.
- Во время ввода допускается временное превышение суммы (для удобства набора). Строгое равенство сумме 1.0 проверяется при нажатии «Утвердить» для каждой таблицы.
- Ввод долей ограничен диапазоном [0..1], поддерживаются точка и запятая, числа форматируются без хвостовых нулей (0.800000 → 0.8).
- После утверждения обеих таблиц становится доступна кнопка «Построить графики» (в одну строку со переключателем «Разделить компоненты по графикам»).
- Графики строятся либо в виде трёх общих осей (Q, σ, K) в основном окне, либо в разделённом виде (по строке на компонент) — в отдельном боковом окне. Основное окно при этом остаётся доступным для редактирования.
- При закрытии бокового окна галочка «Разделить…» автоматически снимается; разделение можно включить снова после изменений и пересчёта.
Если смеси в основном окне ещё не заданы, используются безопасные заглушки (1 компонент с долей 1.0) — окно всё равно откроется.

## Импорт / Экспорт
Меню «Файл» поддерживает:
- Импорт / экспорт JSON входных данных.
- Импорт / экспорт Excel (.xlsx). Значения не преобразуются в даты, разделители десятичной части сохраняются.

### База компонентов (справочник)
- Где хранится: встроенный справочник `COMPONENT_DB` в `interface.py`.
- Через меню: `Файл → Импорт базы компонентов (Excel)…` и `Файл → Экспорт базы компонентов (Excel)…`.
- Поддерживаемый формат Excel:
	- Лист: `components` (если нет — берётся первый лист).
	- Колонки (регистр и язык не критичны, допускаются варианты):
		- `имя` или `name` — название компонента
		- `Tb_K` или `Tb` — температура кипения, K
		- `Cf_kJ_per_kgK` / `C_f` / `Cf` — теплоёмкость жидкой фазы, кДж/(кг·К)
		- `Cp_kJ_per_kgK` / `C_p` / `Cp` — теплоёмкость паровой фазы, кДж/(кг·К)
		- `rf_kJ_per_kg` / `r_f` / `rf` — теплота парообразования, кДж/кг
		- `source_url` — (опционально) ссылка-источник
- Слияние при импорте: существующие записи дополняются/обновляются непустыми значениями из файла.
- Автозагрузка при старте: если существует `data/components.xlsx`, он будет загружен автоматически. При отсутствии — пытаемся прочитать `data/components.csv` или `data/components_nist_results.csv` (только для ссылок-источников).
- CSV-экспорт базы компонентов удалён умышленно. Для обмена используйте Excel.

## Логи
Все действия фиксируются в `logs/app.log`. Просмотр через меню «Помощь → Логи».

## Резервное копирование
Скрипт `backup.ps1`:
- Создаёт каталог `<имя_проекта>_backups` рядом с проектом (или указанный `-BackupRoot`).
- Формирует подпапку по таймстемпу.
- Пишет строку статуса в `backup_history.log`.
- Исключает: `.venv`, `__pycache__`, `.git`, временные каталоги и сам каталог бэкапов.

Пример ручного запуска:
```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\backup.ps1 -Keep 30
```

Планировщик (каждые 30 минут):
```powershell
schtasks /Create /TN HeatSimBackup /TR "pwsh -NoProfile -ExecutionPolicy Bypass -File C:\path\to\backup.ps1" /SC MINUTE /MO 30 /F
```

## Сборка .exe (Windows)

- Проверить, что `PyQt5` установлен в окружении:

```powershell
python -m pip show PyQt5
```

- Быстрая проверка импорта:

```powershell
python -c "from PyQt5 import QtWidgets; print('PyQt5 OK')"
```

- Если при запуске `main.py` возникает `ModuleNotFoundError: No module named 'PyQt5'`, убедитесь, что активировали `.venv` и установили зависимости с помощью `pip install -r requirements.txt`.

**6. Версионность и git**

- `.venv` не должен попадать в репозиторий. Добавьте в `.gitignore` запись:

```
.venv/
```

- `requirements.txt` уже обновлён и отражает установленный набор пакетов и версий.

**7. Частые команды**

- Активировать окружение (PowerShell):
```powershell
.\.venv\Scripts\Activate.ps1
```
- Деактивировать окружение:
```powershell
deactivate
```
- Установить пакет вручную:
```powershell
python -m pip install PyQt5
```

Если хотите, могу:
- добавить `.gitignore` (с исключением `.venv`) и закоммитить изменения, или
- настроить `launch.json`/`settings.json` для VS Code, чтобы проект использовал `.venv` автоматически.

## Выпуск и релизы

- Ручной релиз: используйте `upload-release.ps1`:
```powershell
.\build_exe.ps1
.\upload-release.ps1 -Tag "v1.1" -Publish
```
Скрипт запросит GitHub PAT, если не задан `GITHUB_TOKEN`. Ассет — `dist\HeatSim.exe`.

- Автосборка по тэгу: создан workflow `.github/workflows/release.yml`. При пуше тега вида `v*` CI собирает exe и публикует релиз с ассетом.

## Подпись исполняемого файла

Подготовьте PFX с код‑подписью и установите Windows SDK (`signtool.exe`). Затем:
```powershell
$pwd = Read-Host -AsSecureString "PFX password"
.\sign_exe.ps1 -PfxPath 'C:\path\to\yourcert.pfx' -PfxPassword $pwd
```
Если `signtool.exe` не в PATH, откройте скрипт и пропишите полный путь.

---

Если нужно, допишу README на английском или добавлю пример CSV для импорта/экспорта смесей. Скажите, какие ещё разделы добавить.

## Публикация релиза

В проект добавлен скрипт `build_exe.ps1`, который упрощает упаковку приложения в один исполняемый файл с помощью `PyInstaller`.

Примеры использования (в PowerShell, в корне проекта):

1) Быстрая сборка в текущем окружении:

```powershell
.\build_exe.ps1
```

2) Создать виртуальное окружение, активировать и собрать:

```powershell
.\build_exe.ps1 -CreateVenv
```

После успешной сборки исполняемый файл будет находиться в `dist\HeatSim\HeatSim.exe`.

Примечание: имя итогового exe может отличаться — в некоторых сборках используется `Teploobmen.exe`. Проверьте содержимое папки `dist` после сборки для точного имени и пути.

Советы при проблемах:
- Запустите exe из PowerShell: `& .\dist\HeatSim\HeatSim.exe` чтобы увидеть сообщения об ошибках.
- Если приложение не стартует из-за отсутствия Qt-плагинов, попробуйте собрать без `--windowed` или добавить нужные плагины вручную.

## Отладка

В проекте есть скрипт `upload-release.ps1`, который создаёт/обновляет релиз и загружает сборку в Releases.

Примеры использования:

```powershell
# Запустите в корне проекта; скрипт попросит PAT, если он не передан через параметр или переменную окружения
.\upload-release.ps1 -Token '<YOUR_PAT>'

# или интерактивно (PAT будет запрошен):
.\upload-release.ps1

# Чтобы опубликовать релиз сразу используйте флаг:
.\upload-release.ps1 -Publish
```


